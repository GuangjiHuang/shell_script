1.(x) type: 50 words;

2.(x) English: cpt 75;

3.(x) c/c++: 
1) about the rdbuf, see the document, and then write the program to test it!
2) see the relation of the system(xxx) call funtion and the errno! Try to test it!
3) make conclusion about the iostream and fstream's get/put char, get/put string's method.
4) toolchain: test the use the gcc to make the shared library!
5) write a program to save the image's rectangle part that you select!

4.(x) Qt: 
1) write a program test the sigal and the slot, and the connect function. 
2) 默写qt中的cmake构建的部分代码。

5.(x) TKDNN:
1) 找自己的训练出来的模型权重，跑一遍这个过程。完整的。
2) 写一个完成可以测试自己的视频的c++的子程序。包括有和没有tkdnn的，参考一下之前写的。调用一下darknet的库。对比一下它们对的速度。

6.(x) YOlO_fastest: 
1) try to see the documentation, and then learn how to use it! 
2) try to deploy on the  pc and then run the code.

7.(x) Tracking code: 
1) 看下卡尔曼滤波的原理； 找到opencv中的代码运行和调试一下先； 项是怎样应用的。

8.(y) work: 
1) btree: 前中后序遍历, 重新默写一遍。

9.(x) cmake: 
1) About the qt's package found, review again!

10.(x) work manager:
1) add the function, that can copy the yesterday's task to the today!
2) add the xxx.sh, copy the local script to the dear's pc!

11.(x) shell:

