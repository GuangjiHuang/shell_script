1.(x) 完成topic1、topic2以及question增删部分的的代码。测试好，特别是增加部分的代码。

2.(y) 修复study和其它filetype进行切换时的bug。

3.(x) 当显示的问题的数量太多的时候，自动调用less进行显示，设置一个阈值。

4.(x) 写一个函数，对answer中的问题序号和js中的问题对应起来，并且进行整理。

5.(y) 增加gui的主窗口的移动。

6.(y) 增加一个主gui的隐藏功能，就是缩小主窗口的像素的值。

7.(y) gui去掉进入到record填写的确认环节。

